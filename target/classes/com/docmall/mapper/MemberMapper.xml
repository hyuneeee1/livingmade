<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.docmall.mapper.MemberMapper">

	<!-- 파라미터 타입 전부 생략 가능 -->
	<insert id="join_ok" parameterType="com.docmall.domain.MemberVO">
		INSERT INTO TBL_MEMBER( MEM_NAME, MEM_ID, MEM_PW, MEM_PHONE, MEM_EMAIL, MEM_NICKNAME ) 
		VALUES ( #{mem_name}, #{mem_id}, #{mem_pw}, #{mem_phone}, #{mem_email}, #{mem_nickname}  )
	</insert>
	<select id="idCheck" parameterType="String" resultType="String">
		SELECT MEM_ID 
		FROM TBL_MEMBER 
		WHERE MEM_ID = #{mem_id}
	</select>

</mapper>